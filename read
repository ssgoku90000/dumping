AAAAC3NzaC1lZDI1NTE5AAAAIJFjm1yLvGbG/hne51E6bW0T6s1RDzNkMBPUDRNFwR8R
You are a senior DevOps auditor. I need my self-hosted GitLab CI/CD to deploy from a runner host to two Linux VMs (dev/test), currently on Proxmox.

Task:
1) Tell me exactly what data to collect from each VM and from the GitLab host.
2) Generate a final CI variable map for GitLab:
   DEV_HOST, DEV_PORT, DEV_USER, DEV_DEPLOY_PATH
   TEST_HOST, TEST_PORT, TEST_USER, TEST_DEPLOY_PATH
   SSH_PRIVATE_KEY, SSH_KNOWN_HOSTS
   DBT_PROJECT_DIR, DBT_PROFILES_DIR
   POSTGRES_DEV_URL, POSTGRES_TEST_URL
   CLICKHOUSE_DEV_URL, CLICKHOUSE_TEST_URL
   ATLAS_DEV_ENV, ATLAS_TEST_ENV
3) Identify network/firewall/port changes needed in Proxmox and guest OS firewalls.
4) Validate whether my current GitLab container port bindings are correct for my topology.
5) Return:
   - “Missing info” checklist
   - “Exact commands to run” on GitLab host, Dev VM, Test VM
   - “Ready/Not Ready” verdict with blockers
   - A final table of values ready to paste into GitLab CI/CD variables

My current GitLab setup:
- GitLab web is mapped to host port 8929
- GitLab SSH is mapped to host port 2222
- Compose currently binds to 127.0.0.1 for both ports
- Runner executes on the GitLab host and deploys via SSH to Dev/Test VMs
- VMs are on Proxmox local network now, may be remote VPS later

Important:
- Do not suggest destructive DB schema apply actions.
- Prefer least-privilege and secure defaults.
- Include host key collection using ssh-keyscan.
--

DEV_HOST	<DEV_VM_IP_OR_FQDN>
DEV_PORT	22
DEV_USER	gitlab-cicd
DEV_DEPLOY_PATH	/opt/data_engineering_stack
TEST_HOST	<TEST_VM_IP_OR_FQDN>
TEST_PORT	22
TEST_USER	gitlab-cicd
TEST_DEPLOY_PATH	/opt/data_engineering_stack
SSH_PRIVATE_KEY	<contents of /etc/gitlab-runner/keys/deploy_ed25519>
SSH_KNOWN_HOSTS	<contents of /tmp/known_hosts.ci>
DBT_PROJECT_DIR	client_repo/dbt
DBT_PROFILES_DIR	client_repo/dbt
POSTGRES_DEV_URL	postgresql://<user>:<pass>@<dev-host>:5432/<db>?sslmode=disable
POSTGRES_TEST_URL	postgresql://<user>:<pass>@<test-host>:5432/<db>?sslmode=disable
CLICKHOUSE_DEV_URL	clickhouse://<user>:<pass>@<dev-host>:9000/<db>
CLICKHOUSE_TEST_URL	clickhouse://<user>:<pass>@<test-host>:9000/<db>
ATLAS_DEV_ENV	postgres (or your custom env name)
ATLAS_TEST_ENV	postgres (or your custom env name)
Security settings in GitLab UI:

Mark SSH_PRIVATE_KEY, DB URLs as Masked + Protected
Mark SSH_KNOWN_HOSTS as Protected (mask optional)
Prefer File-type variables for SSH_PRIVATE_KEY and SSH_KNOWN_HOSTS where possible.
